version: "3"

services:
  email:
    build:
      context: ../
      dockerfile: infrastructure/email_microservice/Dockerfile.dev
    container_name: email_microservice
    volumes:
      - "../report:/var/www/html/email_microservice/report"
    environment:
      WAIT_HOSTS: rabbitmq.dev:5672
    restart: always

  email_consumer:
    build:
      context: ../
      dockerfile: infrastructure/email_microservice_consumer/Dockerfile.dev
    container_name: email_microservice_consumer
    environment:
      WAIT_HOSTS: rabbitmq.dev:5672
    restart: always

  nginx:
    build:
      context: ../
      dockerfile: infrastructure/nginx/Dockerfile
    container_name: email_nginx
    restart: always
    ports:
      - "8080:80"

  rabbitmq:
    image: rabbitmq:3.8-rc-management-alpine
    container_name: email_rabbitmq
    networks:
      default:
        aliases:
          - rabbitmq.dev
    restart: always
    ports:
      - "15672:15672"
      - "5672:5672"
